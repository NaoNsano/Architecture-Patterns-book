[[chapter_05_high_gear_low_gear]]
== TDD in High Gear and Low Gear

((创新型发展(TDD),,id=TDix TDDTD)))
We Weve introduced the service layer to capture some of the additional
我们需要从工作上履行职责。 服务层帮助我们
明确界定我们使用的案例和工作流程:
我们需要从我们的储存库、检查前的哪些因素和目前的状态得到帮助。
我们应该做的验证,以及我们最终节省的东西。

((testtest-driven development (TDD)TD, "unit tests operating at lower level, acting directly on model"))
但是,目前,我们的许多单位试验在较低级别进行,采取行动
Direct on the model. 在本章中,我们讨论权衡问题
参与将这些测试提升到服务级;
较一般性的测试准则。


.Harry Says: Seeing a Test Pyramid in Action Was a Light-Bulb Moment
*******************************************************************************
((testtest-driven development (TDD)TD, testtest pyramid, examining examining)))
从哈里里直接讲:

I was initially skeptical of all Bobturs architectural patterns, but seeing
实际试验金字塔使我变成了 convert。

 你执行域模型和服务层,你实际上可以
进入一个阶段,单位测试比合并和最终用户测试数量多
a order of magnitude. 在E2E测试建筑的场所工作
(wawait &quot; #x27;til tomorrow, essentially essentially)
其差数可以用分钟或 second子进行所有试验。

Read on for some guidelines on how to decide what kinds of tests to write
and at which level. The high gear versus low gear way of thinking really changed
I testing life. 
*******************************************************************************


=== How Is Our Test Pyramid Looking?

((serviceservice layer",usingusing, test pyramid and")))
((创新型发展(TDD)test,testtest pyramid with service layer added")))
Let Lets see what this move to using a service layer, with its own service-layer tests,
对我们的试验金字塔:

[[test_pyramid]]
.强制性测试类型
====
[source,sh]
[role="skip"]
----
$ grep -c test_ */*/test_*.py
tests/unit/test_allocate.py:4
tests/unit/test_batches.py:8
tests/unit/test_services.py:3

tests/integration/test_orm.py:6
tests/integration/test_repository.py:2

tests/e2e/test_api.py:2
----
====

//NICE-TO-HAVE: test listing this too?

Not bad! 我们有15次单位测试、8次一体化测试,只有2次最终用户测试。 Thatss
已经是健康的金字塔。



=== Domain Layer Tests Move to the 服务层（Service Layer）?

((dodomain layer layer,testtests moving to service layer")))
((serviceservice layer",dodomain layer tests moving to"))
((testtest-driven development (TDD)TD, dodomain layer tests moving to service layer")))
如果我们进一步采取这一步骤,让我们看到什么情况。 Since we can test our
对照服务层软件,我们确实需要对域进行测试
更进一步的模式。 Instead, we could rewrite all of the domain-level tests from
<<chapter_01_domain_model>> in terms of the service layer:


.Rewriting a domain test at the service layer (tests/unit/test services.py)
====
[source,python]
[role="skip"]
----
# domain-layer test:
def test_prefers_current_stock_batches_to_shipments():
    in_stock_batch = Batch("in-stock-batch", "RETRO-CLOCK", 100, eta=None)
    shipment_batch = Batch("shipment-batch", "RETRO-CLOCK", 100, eta=tomorrow)
    line = OrderLine("oref", "RETRO-CLOCK", 10)

    allocate(line, [in_stock_batch, shipment_batch])

    assert in_stock_batch.available_quantity == 90
    assert shipment_batch.available_quantity == 100


# service-layer test:
def test_prefers_warehouse_batches_to_shipments():
    in_stock_batch = Batch("in-stock-batch", "RETRO-CLOCK", 100, eta=None)
    shipment_batch = Batch("shipment-batch", "RETRO-CLOCK", 100, eta=tomorrow)
    repo = FakeRepository([in_stock_batch, shipment_batch])
    session = FakeSession()

    line = OrderLine('oref', "RETRO-CLOCK", 10)

    services.allocate(line, repo, session)

    assert in_stock_batch.available_quantity == 90
    assert shipment_batch.available_quantity == 100
----
====

((dodomain layer layer,testtests moving to service layer", reareasons for for)))
((serviceservice layer","domain layer tests moving to", reareasons for for)))
为什么我们要这样做?

测试本应帮助我们无情地改变我们的制度,但往往
We see teams writing too many tests against their domain model. 原因
在改变编码时出现问题,发现需要
更新数百次甚至数百次单位测试。

如果你不考虑自动试验的目的,这一点就具有意义。 我们
Use tests to enforce that a property of the system doesn changet change while werere
Work. We use tests to check that the API continues to return 200, that the
数据库会议继续承诺,订单仍在分配中。

如果我们意外地改变其中一种行为,我们试验就会破裂。 The
但是,一边是,如果我们想要改变我们代码的设计,那么,任何一方都不会改变。
直接依赖该代码的测试也将失败。

在我们进一步加入该书时,你会看到服务层是如何形成API的。
对我们能够以多种方式推动的系统而言。 Testing against this API
减少在我们重新确定我们的域时我们需要改变的编码数量
模式。 如果我们仅限于对服务层的测试,
We wonont have any tests that directly interact with privateprivateprivate methods or
我们的示范物体的属性使我们更自由地重新制造。

TIP: Every line of code that we put in a test is like a blob of glue, holding
    the system in a particular shape. 我们进行的低级试验,即:
    更难改变事情。


[[kinds_of_tests]]
=== On Deciding What Kind of Tests to Write

((dodomain model", cideciding whether to write tests against against))
((cocoupling", tradetrade-off between design feedback and"))
((testtest-driven development (TDD)TD, cideciding what kinds of tests to write"))
你也许会问你自己,那么,我是否会重新撰写所有的单位测试? Is it
对照域模型写作测试的错误? 回答这些问题
了解加权与设计反馈之间的权衡(见
<<test_spectrum_diagram>>).

[[test_spectrum_diagram]]
.测试频 The
image::images/apwp_0501.png[]
[role="image-source"]
----
[ditaa, apwp_0501]
| Low feedback                                                   High feedback |
| Low barrier to change                                 High barrier to change |
| High system coverage                                        Focused coverage |
|                                                                              |
| <---------                                                       ----------> |
|                                                                              |
| API Tests                  Service–Layer Tests                  Domain Tests |
----


((极端方案规划(XP),hor听守则的吸引力))
Extreme programming (XP) exhorts us to listlisten to the code." When we writingre writing
我们也许会发现,该代码很难使用或通知代号。 This
这是我们重新制造和重新考虑我们设计的诱因。

然而,我们只有得到这种反馈,当我们与目标密切合作时
code. A test for the HTTP API tells us nothing about the fine-grained design of
我们的物体,因为它的抽象程度要高得多。

另一方面,只要我们能够重写我们的全部申请,我们就能够重新写好。
dont change the URLs or request formats, our HTTP tests will continue to pass.
这使我们确信,大规模变革,如改变数据库的序列,
haven.t broken our code.

在另一端,我们在<<chapter_01_domain_model>>中写到的测试帮助了我们
我们清楚地了解我们需要的物体。 试验指导我们进行
Design that makes sense and reads in the domain language. 当我们的试验改为:
在域语中,我们感到舒适的是,我们的守则与我们的教学相匹配。
about the problem we tryingre trying to solve.

由于测试是用域内语言写成的,因此它们是活着
Documentation for our model. 新的小组成员可以迅速阅读这些测试
了解该系统如何运作,核心概念如何相互关联。

We often ketsketchch new behaviors by writing tests at this level to see how the
守则可以看。 然而,如果我们要改进守则的设计,我们就需要取代
或删除这些试验,因为这些试验与特定试验密切相关
[彻底]#implementation#.

// IDEA: (EJ3) an example that is overmocked would be good here if you decide to
// add one. Ch12 already has one that could be expanded.

// IDEA (SG) - maybe we could do with a/some concrete examples here?  Eg an
// example where a unit test would break but a service-layer test wouldn't?
// and maybe make the analogy of "you should only write tests against public
// methods of your classes, and the service layer is just another more-public
// layer


=== High and Low Gear

((testtest-driven development (TDD)TD, highhigh and low gear")))
大部分时间,当我们正在增加新的特征或确定一个ug时,我们就会don。
需要对域模型进行广泛的修改。 在这些情况下,我们宁愿
to write tests against services because of the lower coupling and higher coverage.

((serviceservice layer",wwriting tests against"))
例如,在撰写`add_stock`功能或`cancel_order`功能时,
我们能够更快地工作,通过对书面试验减少加倍
服务层。

((dodomain model", wwriting tests against"))
When starting a new project or when hitting a particularly gnarly problem,
We will drop back down to writing tests against the domain model so we
获得关于我们意图的更好的反馈和可执行的文件。

我们使用的是移动设备。 启程时
自行车需要低容量,以克服ert性。 Once werere off
我们能够更快、更有效率地投入运作,改变为高速装备;
但是,如果我们突然遇到te山,或被迫放慢
危险再次降到低 gear,直到我们能够再次加快速度。



[[primitive_obsession]]
=== Fully Decoupling the Service-Layer Tests from the Domain

((serviceservice layer","fully decoupling from the domain", id="ix serlaydec"))
((dodomain layer", fullyfully decoupling service layer from", id="ix domlaydec"))
((由试验驱动的发展(TDD)driven,fullyfully decoupling service layer from the domain", id=TDix TDDdecserTD)))
我们仍然直接依赖我们服务领域。
因为我们使用域物体来确定我们的试验数据并加以利用
我们的服务层级职能。

为了使服务层完全脱离这一领域,我们需要
Rewrite its API to work in terms of primitives.

我们的服务层目前有一个`OrderLine`域物体:

[[service_domain]]
.在分配之前,用域物体(服务/服务.间)
====
[source,python]
[role="skip"]
----
def allocate(line: OrderLine, repo: AbstractRepository, session) -> str:
----
====

How would it look if its parameters were all primitive types?

[[service_takes_primitives]]
.离职后:分配数和帐ts(服务/服务/间))
====
[source,python]
----
def allocate(
    orderid: str, sku: str, qty: int,
    repo: AbstractRepository, session
) -> str:
----
====

我们还重写了这些试验:

[[tests_call_with_primitives]]
.测试目前在功能呼叫(测试/无用/测试服务.间))中使用原始设备
====
[source,python]
[role="non-head"]
----
def test_returns_allocation():
    batch = model.Batch("batch1", "COMPLICATED-LAMP", 100, eta=None)
    repo = FakeRepository([batch])

    result = services.allocate("o1", "COMPLICATED-LAMP", 10, repo, FakeSession())
    assert result == "batch1"
----
====

但是,我们的试验仍然取决于这个领域,因为我们仍然以人工方式证明。
`Batch`物体。 因此,如果我们有一天决定大规模重新确定我们`Batch`如何
模型工程必须改变试验的un。


==== Mitigation: Keep All Domain Dependencies in Fixture Functions

((fafaking", FFake 仓储（Repository）", adadding fixture function on"))
((ixfixture functions, keeping all domain dependencies in"))
((由试验驱动的发展(TDD)driven,有意使服务层与领域脱钩co,在固定职能中保持所有域的依赖性"))
((独立、保留固定职能中的所有域属 depend))
We could at least abstract that out to a helper function or a fixture
in our tests. 你可以这样做的一种方式,加上一个工厂
function on `Fake仓储（Repository）`:


[[services_factory_function]]
.固定装置的工厂功能是一种可能性(试验/无声/试验性服务。
====
[source,python]
[role="skip"]
----
class FakeRepository(repository.AbstractRepository):

    @staticmethod
    def for_batch(ref, sku, qty, eta=None):
        return FakeRepository([
            model.Batch(ref, sku, qty, eta),
        ])

    ...


def test_returns_allocation():
    repo = FakeRepository.for_batch("batch1", "COMPLICATED-LAMP", 100, eta=None)
    result = services.allocate("o1", "COMPLICATED-LAMP", 10, repo, FakeSession())
    assert result == "batch1"
----
====


至少这将改变我们的所有试验 depend取决于领域
into one place.


==== Adding a Missing Service

((testtest-driven development (TDD)TD, fullyfully decoupling service layer from the domain", "adding missing service")))
然而,我们可以进一步迈出一步。 如果我们有增加库存的服务,
我们可以使用这一手段,并充分表达我们的服务延期试验。
就服务层而言,正式使用案件,消除所有依赖性
on the domain:


[[test_add_batch]]
.新添加剂服务试验(试验/无用/测试服务.间)
====
[source,python]
----
def test_add_batch():
    repo, session = FakeRepository([]), FakeSession()
    services.add_batch("b1", "CRUNCHY-ARMCHAIR", 100, None, repo, session)
    assert repo.get("b1") is not None
    assert session.committed
----
====


TIP: In general, if you find your themselves needing to do domain-layer stuff directly
    在你的服务工时测试中,可以说明你的任职情况
    层级不完整。

[role="pagebreak-before"]
而执行只是两个方面:

[[add_batch_service]]
.添加剂的新服务(服务/服务/服务.间)
====
[source,python]
----
def add_batch(
    ref: str, sku: str, qty: int, eta: Optional[date],
    repo: AbstractRepository, session,
) -> None:
    repo.add(model.Batch(ref, sku, qty, eta))
    session.commit()


def allocate(
    orderid: str, sku: str, qty: int,
    repo: AbstractRepository, session
) -> str:
----
====

NOTE: If you write a new service just because it would help remove
    你的测试是否可靠? Probably not. 但在这种情况下,我们
    几乎肯定需要一天的`add_batch`服务(全天)#anyway#。

((serviceservices",serviceservice layer tests only using services"))
现在,我们只能重写我们服务迟延试验的rit。
in terms of the services themselves, using only primitives, and without
模式上的任何依赖性:


[[services_tests_all_services]]
.服务测试现在只使用服务(测试/无用/测试服务.间))
====
[source,python]
----
def test_allocate_returns_allocation():
    repo, session = FakeRepository([]), FakeSession()
    services.add_batch("batch1", "COMPLICATED-LAMP", 100, None, repo, session)
    result = services.allocate("o1", "COMPLICATED-LAMP", 10, repo, session)
    assert result == "batch1"


def test_allocate_errors_for_invalid_sku():
    repo, session = FakeRepository([]), FakeSession()
    services.add_batch("b1", "AREALSKU", 100, None, repo, session)

    with pytest.raises(services.InvalidSku, match="Invalid sku NONEXISTENTSKU"):
        services.allocate("o1", "NONEXISTENTSKU", 10, repo, FakeSession())
----
====


((serviceservice layer","fully decoupling from the domain", startref="ix serlaydec"))
((dodomain layer", fullyfully decoupling service layer from", startref="ix domlaydec"))
((由试验驱动的发展(TDD)driven,从领域完全脱钩服务层,开始=TDix TDDdecserTD)))
这是一个真正有活力的地方。 我们的服务工时测试只取决于
服务层本身,使我们完全自由地改变模式
我们认为是合适的。

[role="pagebreak-before less_space"]
=== 通过E2E测试进行改进

((E2E测试",见=端到端测试"))
((end端到端测试,,从领域划分服务层,结转至 carrying))
((testtest-driven development (TDD)TD, fullyfully decoupling service layer from the domain", "carrying improvement through to E2E tests")))
((PIAPIsPI, adadding API for adding a batch"))
In the same way that adding `add_batch` helped decouple our service-layer
从模型中得出的试验,加上API的终点,以添加一块atch,将消除
需要微量的`add_stock`装置,我们的E2E试验可以是免费的
of those hardcoded SQL queries and the direct dependency on the database.

由于我们的服务功能,增加最终点是容易的,只有很小一点
JSON wrangling and a single function call required:


[[api_for_add_batch]]
.API for adding a batch (entrypoints/flask app.py)
====
[source,python]
----
@app.route("/add_batch", methods=["POST"])
def add_batch():
    session = get_session()
    repo = repository.SqlAlchemyRepository(session)
    eta = request.json["eta"]
    if eta is not None:
        eta = datetime.fromisoformat(eta).date()
    services.add_batch(
        request.json["ref"],
        request.json["sku"],
        request.json["qty"],
        eta,
        repo,
        session,
    )
    return "OK", 201
----
====

NOTE: Are you thinking to yourself, POST to  /add batch ? 不
    very RESTful! You.re quite right. We Were being happily sloppy, but
    If youSTd like to make it all more RESTy, maybe a POST to  /batches ,
    然后把自己赶上! Because Flask is a thin adapter, it itll be
    容易。 见<<types_of_test_rules_of_thumb, the next sidebar>>。

用某种方式取代了我们从 conftest.py 收到的硬编码的SQL询问。
API calls, meaning the API tests have no dependencies other than the API,
也是 n:

[[api_tests_with_no_sql]]
.API tests can now add their own batches (tests/e2e/test api.py)
====
[source,python]
----
def post_to_add_batch(ref, sku, qty, eta):
    url = config.get_api_url()
    r = requests.post(
        f"{url}/add_batch", json={"ref": ref, "sku": sku, "qty": qty, "eta": eta}
    )
    assert r.status_code == 201


@pytest.mark.usefixtures("postgres_db")
@pytest.mark.usefixtures("restart_api")
def test_happy_path_returns_201_and_allocated_batch():
    sku, othersku = random_sku(), random_sku("other")
    earlybatch = random_batchref(1)
    laterbatch = random_batchref(2)
    otherbatch = random_batchref(3)
    post_to_add_batch(laterbatch, sku, 100, "2011-01-02")
    post_to_add_batch(earlybatch, sku, 100, "2011-01-01")
    post_to_add_batch(otherbatch, othersku, 100, None)
    data = {"orderid": random_orderid(), "sku": sku, "qty": 3}

    url = config.get_api_url()
    r = requests.post(f"{url}/allocate", json=data)

    assert r.status_code == 201
    assert r.json()["batchref"] == earlybatch
----
====


=== Wrap-Up

((服务层 layer,测试驱动发展的效益benefit))
((创新型发展(TDD),,服务层的效益benefit)))
Once you have a service layer in place, you really can move the majority
of your test coverage to unit tests and develop a healthy test pyramid.

[role="nobreakinside less_space"]
[[types_of_test_rules_of_thumb]]
.Recap: Rules of Thumb for Different Types of Test
******************************************************************************

每种特征的最终测试:
    This might be written against an HTTP API, for example. 目标
    证明该特征行之有效,所有移动部分
    together在一起是正确的。
    ((endend-to-end tests", "aiming for one test per feature"))

撰写了大部分对服务层的测试:
    这些按地对地的试验在覆盖范围之间提供了良好的权衡,
    运行时间和效率。 每种试验往往包括一种代码道路
    I/O. 特征和使用 fa。 这是详尽无遗的地方
    包括所有编辑案件和你的商业逻辑的印记。
    对较高一级书面测试的有效关切是,这可能导致
    用于更复杂的使用案件的同化爆炸。 在这些情况下,辍学
    各种合作领域物体的下级单位试验
    可以是有益的。 But see also <<chapter_08_events_and_message_bus>> and
    <<fake_message_bus>>。 ]
    ((serviceservice layer",wwriting bulk of tests against against))

Maintain a small core of tests written against your domain model:
    These tests have highly focused coverage and are more brittle, but they have
    the highest feedback. 如果这些试验不宜删除
    服务层的测试后涵盖功能。
    ((dodomain model", mainmaintaining small core of tests written against against)))

rr处理计为:
    Ideally, your application will be structured such that all errors that
    你的入境点(如Flask)的布局也以同样的方式处理。
    这就是说,你需要只检验每一特征的快车道,并且要检验每一特征的快车道。
    为所有不喜欢的路径(和许多不喜欢的路径)保留一端到端试验
    单位测试,当然)。
    ((创新型发展(TDD) start,开端=TDix TDDTD)))
    ((ererror handling", countcounting as a feature")))

******************************************************************************

少数
事情将有助于:

* 用原始物品而不是域物体来表达你的服务层。

* 在一个理想的世界中,你需要能够检验的一切服务。
  entirely against the service layer, rather than hacking state via
  储存库或数据库。 这在你的最终到终考试中也是付酬的。
  ((由试验驱动的发展(TDD) development,试验类型,um)

下一章!
