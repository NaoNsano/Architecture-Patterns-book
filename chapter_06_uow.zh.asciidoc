[[chapter_06_uow]]
== 工作单元（Unit of Work）模式

(( &quot; 工作模式单位 &quot; ,id=Uix UoW")))
在本章中,我们应介绍与该联系的结局的最后一块。
together the 仓储（Repository） and 服务层（Service Layer） patterns: the  工作单元（Unit of Work）  pattern.

((UUoW", see=Uni工作单元（Unit of Work） pattern")))
((atoatomic operations"))
如果储存模式是我们对长期储存的想法的抽象,
&quot; 工作单位 &quot; 模式是我们对 &quot; 天体行动 &quot; 想法的抽象。 It
这将使我们能够最终和充分地将我们的服务层与数据层脱钩。

(( &quot; 工作模式单位 &quot; 、 &quot; 无 &quot; 、 &quot; API &quot; 直接与三层 &quot; )))
((PIAPIsPI,"without 工作单元（Unit of Work） pattern, talking directly to three layers")))
<<before_uow_diagram>>显示,目前发生了许多通信活动
across the layers of our infrastructure: the API talks directly to the database
一层开始会议,与储存层进行会谈,以开始
`SQLAlchemy仓储（Repository）`, and it talks to the service layer to ask it to allocate.

[TIP]
====
本章的守则载于
Chapter 06 uow branch https://oreil.ly/MoWdZ[on [.keep-together]#GitHub#]:

----
git clone https://github.com/cosmicpython/code.git
cd code
git checkout chapter_06_uow
# or to code along, checkout Chapter 4:
git checkout chapter_04_service_layer
----
====

[role="width-75"]
[[before_uow_diagram]]
.Without UoW: API talks directly to three layers
image::images/apwp_0601.png[]

((datadatabases",Uni工作单元（Unit of Work） pattern managing state for for))
((工作模式单位,管理数据库国 state))
<<after_uow_diagram>>显示了我们的目标状态。 The Flask API now does only two
事 件:它开始工作单位,并援引服务。 服务
与UoW(我们想把UoW看作是UoW的一部分)合作
服务层,但无论是服务功能本身还是Flask现在都不需要
to talk directly to the database.

((合同经理))
And we well do it all using a lovely piece of Python syntax, a context manager.

[role="width-75"]
[[after_uow_diagram]]
.With UoW: UoW now manages database state
image::images/apwp_0602.png[]


=== The 工作单元（Unit of Work） Collaborates with the 仓储（Repository）

//TODO (DS) do you talk anywhere about multiple repositories?

((reprepositories", "工作单元（Unit of Work） collaborating with"))
((工作模式单位,与存储处合作))
Let Lets see the unit of work (or UoW, which we pronounce "you-wowow) in action. 这里,服务层将如何看待完成时:

[[uow_preview]]
.业务单位审查(rc/allocation/service layer/service.py)
====
[source,python]
----
def allocate(
    orderid: str, sku: str, qty: int,
    uow: unit_of_work.AbstractUnitOfWork,
) -> str:
    line = OrderLine(orderid, sku, qty)
    with uow:  #<1>
        batches = uow.batches.list()  #<2>
        ...
        batchref = model.allocate(line, batches)
        uow.commit()  #<3>
----
====

We1 We We'll公司作为背景经理开始工作。
    ((contcontext manager", ststarting 工作单元（Unit of Work） as as)))

bat2 P `uow.batches`是弹ches,因此,UoW为我们提供了服务
    Access to our permanent storage.
    ((storstorage", permanent permanent, UoW providing entrypoint to"))

we3. When we.re done, we commit or roll our work, using the UoW.

((目标邻居eigh))
((协作者))
The UoW acts as a single entrypoint to our persistent storage, and it
 跟踪装载的物体和最新状态。
You may have come across the use of the word  collaborators  to describe objects that work
共同实现目标。 工作单位和存储处是一个很大的单位
Examples of collaborators in the object-modeling sense.
In responsibility-driven design, cluster of objects that collaborate in their
作用称为 &quot; 客观邻居 &quot; ,这是我们的专业意见,
完全可以否认。 ]

这使我们有三个有益的事物:

* A stable snapshot of the database to work with, so the
   We use aren't changing halfway through an operation

* A way to persist all of our changes at once, so if something
   不错,我们在一个前后不一的状态下结束

* A simple API to our persistence concerns and a handy place
   获得存储处



=== 测试与结合测试

((tegintegration tests","test-driving 工作单元（Unit of Work） with"))
((testtestingtest, "工作单元（Unit of Work） with integration tests"))
((Uni工作单元（Unit of Work） pattern", testtest driving with integration tests"))
这里是我们对普遍义务的一体化试验:


[[test_unit_of_work]]
.A basic "round-triprip test for a UoW (tests/integration/test uow.py)
====
[source,python]
----
def test_uow_can_retrieve_a_batch_and_allocate_to_it(session_factory):
    session = session_factory()
    insert_batch(session, "batch1", "HIPSTER-WORKBENCH", 100, None)
    session.commit()

    uow = unit_of_work.SqlAlchemyUnitOfWork(session_factory)  #<1>
    with uow:
        batch = uow.batches.get(reference="batch1")  #<2>
        line = model.OrderLine("o1", "HIPSTER-WORKBENCH", 10)
        batch.allocate(line)
        uow.commit()  #<3>

    batchref = get_allocated_batch_ref(session, "o1", "HIPSTER-WORKBENCH")
    assert batchref == "batch1"
----
====

o1> 我们利用我们的定制会议工厂开始 U瓦
    and get back a `uow` object to use in our `with` block.

U2 The The UoW gives us access to the batches Library via
    `uow.batches`.

<3. We call `commit()` on it when we.re done.

((SQLQ,"helpers for 工作单元（Unit of Work）"))
For the curious, the `insert_batch` and `get_allocated_batch_ref` helpers look
例如:

[[sql_helpers]]
.帮助做SQL stuff (tests/integration/test uow.py)
====
[source,python]
----
def insert_batch(session, ref, sku, qty, eta):
    session.execute(
        "INSERT INTO batches (reference, sku, _purchased_quantity, eta)"
        " VALUES (:ref, :sku, :qty, :eta)",
        dict(ref=ref, sku=sku, qty=qty, eta=eta),
    )


def get_allocated_batch_ref(session, orderid, sku):
    [[orderlineid]] = session.execute(  #<1>
        "SELECT id FROM order_lines WHERE orderid=:orderid AND sku=:sku",
        dict(orderid=orderid, sku=sku),
    )
    [[batchref]] = session.execute(  #<1>
        "SELECT b.reference FROM allocations JOIN batches AS b ON batch_id = b.id"
        " WHERE orderline_id=:orderlineid",
        dict(orderlineid=orderlineid),
    )
    return batchref
----
====

P1. The `[[orderlineid]] =` syntax is a little too-clever-by-half, apologies.
    What's happening is that `session.execute` returns a list of rows,
    每一行是各栏值的基数;
    in our specific case, it its a list of one row,
    其中一栏价值。
    The double-square-bracket on the left hand side
    正在进行(双重)外派包装,以获取单一价值
    back out of these two nested sequences.
    你曾数次使用它!


=== 工作及其背景经理

(( &quot; 工作模式单位 &quot; 、 &quot; 及其背景经理 &quot; ))
((contcontext manager", Uni工作单元（Unit of Work） and", id="ix ctxtmgrgr)))
((bstractionsction, "AbstractUnitOfWork")))
In our tests we weve implicitly defined an interface for what a UoW needs to do. Let abstracts make that explicit by using an abstract
基数:


[[abstract_unit_of_work]]
.Abstract UoW context manager (src/allocation/service layer/unit of work.py)
====
[source,python]
[role="skip"]
----
class AbstractUnitOfWork(abc.ABC):
    batches: repository.AbstractRepository  #<1>

    def __exit__(self, *args):  #<2>
        self.rollback()  #<4>

    @abc.abstractmethod
    def commit(self):  #<3>
        raise NotImplementedError

    @abc.abstractmethod
    def rollback(self):  #<4>
        raise NotImplementedError
----
====

o1 The The UoW provides an attribute called `.batches`, which will give us access
    to the batches store.

you2, If you've never seen a context manager, +++<code>__enter__</code>+++ and +++<code>__exit__</code>+++ are
    在我们进入`with`区和
    当我们分别退出时。 我们的建立和破碎阶段。
    ((magmagic methods", &еx5f;enter#x5f; #x5f; and &quot;#x5f; #x5f;exit#x5f; #x5f; #x5f;#, secondary-sortas=ententer")))
    (((& #x5f; #x5f;enter#x5f;#x5f; #x5f; #x5f;exit#x5f;#x5f; magic methods", primary-sortas=ententer and exit"))))

We3 We We'll call this method to explicitly commit our work when we.re ready.

don4, 如果我们做出承诺,或者如果我们通过挑出错误离开环境经理,
    We do a `rollback`. (如果`commit()`已经出现,则 roll动无效果
    called. Read on for more discussion of this.
    ((回馈))

// TODO: bring this code listing back under test, remove `return self` from all the uows.


==== The Real 工作单元（Unit of Work） Uses SQLAlchemy Sessions

(( &quot; 工作模式单位 &quot; 、 &quot; 及其背景经理 &quot; 、 &quot; 利用SQLAlchemy会议 &quot; ))
((datadatabases","SQLAlchemy 增加工作单位的会议)))
((工作股数据库会议))
我们具体执行所增加的主要内容是:
数据库会议:

[[unit_of_work]]
.The real SQLAlchemy UoW (src/allocation/service layer/unit of work.py)
====
[source,python]
----
DEFAULT_SESSION_FACTORY = sessionmaker(  #<1>
    bind=create_engine(
        config.get_postgres_uri(),
    )
)


class SqlAlchemyUnitOfWork(AbstractUnitOfWork):
    def __init__(self, session_factory=DEFAULT_SESSION_FACTORY):
        self.session_factory = session_factory  #<1>

    def __enter__(self):
        self.session = self.session_factory()  # type: Session  #<2>
        self.batches = repository.SqlAlchemyRepository(self.session)  #<2>
        return super().__enter__()

    def __exit__(self, *args):
        super().__exit__(*args)
        self.session.close()  #<3>

    def commit(self):  #<4>
        self.session.commit()

    def rollback(self):  #<4>
        self.session.rollback()
----
====

module1 The 模块界定了将连接度表的缺省会议工厂,
    但是,我们允许在一体化试验中压倒一切,以便我们能够这样做
    可以使用SQLite。

+2 The++<code>__enter__</code>+++法负责启动数据库会议和调查
    a 可使用本届会议的真实资料库。
    (((& #x5f; #x5f;enter#x5f;#x5f; #x5f; #x5f;exit#x5f;#x5f; magic methods", primary-sortas=ententer and exit"))))

<3. 我们结束退出会议。

<4, Finally, we provide concrete `commit()` and `rollback()` methods that
    利用我们的数据库会议。
    ((commcommits", commcommit method"))
    ((rollrollbacks", rollrollback method"))

//IDEA: why not swap out db using os.environ?
// (EJ2) Could be a good idea to point out that this couples the unit of work to postgres.
//         This does get dealt with in in bootstrap, so you could make a forward-reference.
// (EJ3) IIRC using a factory like this is considered an antipattern ("Control-Freak" from M.Seeman's book)
//         Is there a reason to inject a factory instead of a session?
// (HP) yes because each unit of work needs to start a new session every time
// we call __enter__ and close it on __exit__



==== 测试工作股

(( &quot; 工作模式单位 &quot; 、 context及其背景经理、fafake UoW for testing"))
((fafaking","FakeUnitOfWork for service layer testing")))
((testtestingtest,"fake UoW for service layer testing"))
这里,我们如何在我们的服务后继试验中使用假肢:

[[fake_unit_of_work]]
.Fake UoW (tests/unit/test services.py)
====
[source,python]
----
class FakeUnitOfWork(unit_of_work.AbstractUnitOfWork):
    def __init__(self):
        self.batches = FakeRepository([])  #<1>
        self.committed = False  #<2>

    def commit(self):
        self.committed = True  #<2>

    def rollback(self):
        pass


def test_add_batch():
    uow = FakeUnitOfWork()  #<3>
    services.add_batch("b1", "CRUNCHY-ARMCHAIR", 100, None, uow)  #<3>
    assert uow.batches.get("b1") is not None
    assert uow.committed


def test_allocate_returns_allocation():
    uow = FakeUnitOfWork()  #<3>
    services.add_batch("batch1", "COMPLICATED-LAMP", 100, None, uow)  #<3>
    result = services.allocate("o1", "COMPLICATED-LAMP", 10, uow)  #<3>
    assert result == "batch1"
...
----
====

tight1 P`FakeUnitOfWork`和`Fake仓储（Repository）`紧密相连,
    Just like the real `UnitofWork` and `仓储（Repository）` classes.
    这会被罚款,因为我们承认这些物体是合作者。

Not2 Not 通知与`commit()`功能相似
    来自`FakeSession`(现在我们可以摆脱)。 But it its
    a 大幅改善,因为我们现在[全面]#faking# out
    我们撰写而不是第三方代码的代码。 Some
    people say, https://oreil.ly/0LVj3["Don't mock what you dondont own.].

U3, 在我们的试验中,我们能够证实一个世界,并把它传给它。
    我们的服务层,而不是通过一个存储处和一届会议。
    这大大减少了麻.。

[role="nobreakinside less_space"]
.Don't Mock What You Don Ot Own
********************************************************************************
((QSQLAlchemyche, Workdatabase session for 工作单元（Unit of Work）",  monot mocking"))
((ockmocking", "don#x27;t mock what you don#x27;t own"))
为什么我们觉得比会议更舒适地ck弄 U。
我们的两面都取得了同样的成就:它们为我们铺平了道路。
persistence固的层层,以便我们能够进行记忆测试,而不是需要
谈到一个真正的数据库。 差异在于由此产生的设计。

如果我们只关心迅速进行的书面试验,我们就能够制造 mo。
这将取代SQLAlchemy,并在整个代码中使用。 问题
`Session`是一个复杂的物体,暴露了许多与持久性有关的物体
功能。 很容易使用`Session`对个人进行任意询问
数据库,但这迅速导致数据访问代码被标出所有数据
over the codebase. 为了避免这种情况,我们想限制我们持续不断的机会
因此,每个组成部分都确实需要什么,没有什么需要。

By coupling to the `Session` interface, you,re choosing to couple to all the
SQLAlchemy的复杂性。 相反,我们想选择更简单的抽象,
Use that to clearly separate responsibilities. 我们的世界更加简单
我们感到舒适于服务层能够举行的会议
开始和停止工作单位。

DDon't mock what you dondont own" is a rule of thumb that forces us to build
These simple abstractions over mesy subsystems. 业绩相同
模拟谢克米会议,但鼓励我们认真考虑
about our designs.
((contcontext manager", Uni工作单元（Unit of Work） and", startref="ix ctxtmgrgr))
********************************************************************************

=== Use the UoW in the 服务层（Service Layer）

(( &quot; 工作模式单位 &quot; ,在服务层中使用UoW)))
((serviceservice layer",usingusing 工作单元（Unit of Work） in"))
这里,我们的新服务层看着:


[[service_layer_with_uow]]
.使用UoW的服务层(机/地点/服务/服务级/服务级间))
====
[source,python]
----
def add_batch(
    ref: str, sku: str, qty: int, eta: Optional[date],
    uow: unit_of_work.AbstractUnitOfWork,  #<1>
):
    with uow:
        uow.batches.add(model.Batch(ref, sku, qty, eta))
        uow.commit()


def allocate(
    orderid: str, sku: str, qty: int,
    uow: unit_of_work.AbstractUnitOfWork,  #<1>
) -> str:
    line = OrderLine(orderid, sku, qty)
    with uow:
        batches = uow.batches.list()
        if not is_valid_sku(line.sku, batches):
            raise InvalidSku(f"Invalid sku {line.sku}")
        batchref = model.allocate(line, batches)
        uow.commit()
    return batchref
----
====

Our1, 我们的服务层现在只有一个受扶养人,
    Once again on an  abstract  UoW.
    ((独立性、对抽象的依赖性))


=== Explicit Tests for Commit/Rollback Behavior

((commcommits", exexplicit tests for for))
((rollrollbacks","explicit tests for for))
((testtestingtest, "integration tests for rollback behavior"))
(( &quot; 工作模式单位 &quot; ,exexplicit tests for commit/rollback behavior"))
为了说服自己,我们写了承诺/结果行为的工作。
a 几种试验:

[[testing_rollback]]
.roll动行为(试验/整合/试验ow)的整合试验
====
[source,python]
----
def test_rolls_back_uncommitted_work_by_default(session_factory):
    uow = unit_of_work.SqlAlchemyUnitOfWork(session_factory)
    with uow:
        insert_batch(uow.session, "batch1", "MEDIUM-PLINTH", 100, None)

    new_session = session_factory()
    rows = list(new_session.execute('SELECT * FROM "batches"'))
    assert rows == []


def test_rolls_back_on_error(session_factory):
    class MyException(Exception):
        pass

    uow = unit_of_work.SqlAlchemyUnitOfWork(session_factory)
    with pytest.raises(MyException):
        with uow:
            insert_batch(uow.session, "batch1", "LARGE-FORK", 100, None)
            raise MyException()

    new_session = session_factory()
    rows = list(new_session.execute('SELECT * FROM "batches"'))
    assert rows == []
----
====

TIP: We haven't shown it here, but it can be worth testing some of the more
    oobscureure database behavior, like transactions, against the "real"
    数据库是同一引.。 我们现在就不再使用
    SQLite instead of Postgres, but in <<chapter_07_aggregate>>, we switchll switch
    Some of the tests to using the real database. It Its convenient that our UoW
    &quot; 难!
    ((datadatabases",testtesting transactions against real database")))


=== Explicit Versus Implicit Commits

((impimplicit versus explicit commits"))
((commcommits", exexplicit versus implicit"))
(( &quot; 工作模式单位 &quot; 、 &quot; 透明与隐含承诺 &quot; )))
现在,我们简要地在落实《世界妇女模式》的不同方式上退步。

We could imagine a slightly different version of the UoW that commits by default
只有在发现有例外的情况下,才能恢复:

[[uow_implicit_commit]]
.A UoW with implicit commit.. (src/allocation/unit of work.py)
====
[source,python]
[role="skip"]
----

class AbstractUnitOfWork(abc.ABC):

    def __enter__(self):
        return self

    def __exit__(self, exn_type, exn_value, traceback):
        if exn_type is None:
            self.commit()  #<1>
        else:
            self.rollback()  #<2>
----
====

happy1 we 我们是否应该在幸福的道路上作出默示承诺?
<2 And And roll only on exception?

这将使我们能够挽救一条守则,消除我们的明确承诺
客户代码:

[[add_batch_nocommit]]
.code..将拯救我们一条代码(机/地点/服务/服务/服务.间))
====
[source,python]
[role="skip"]
----
def add_batch(ref: str, sku: str, qty: int, eta: Optional[date], uow):
    with uow:
        uow.batches.add(model.Batch(ref, sku, qty, eta))
        # uow.commit()
----
====

这是判决,但我们倾向于要求明确承诺
因此,我们必须选择何时动摇。

虽然我们使用代码,但这使软件安全失误。
The default behavior is to  not change anything . 反过来,这又使我们的守则成为现实
容易推测,因为只有一条导致改变的代码途径
in the system: total success and an explicit commit. 任何其他编码途径,
除此以外,任何早期撤出UoW's的范围都会导致一个安全的国家。

同样,我们宁愿以默认方式回来,因为
It,s easier to understand; this rolls back to the last commit,
这样,用户要么做,要么我们打折扣其变化。 Harsh but simple.

=== Examples: Using UoW to Group Multiple Operations into an Atomic Unit

((atoatomic operations", usingusing 工作单元（Unit of Work） to group operations into Atomic unit", id="ix atomops")))
((Uni工作单元（Unit of Work） pattern", "using UoW to group multiple operations into Atomic unit", id="ix UoWatom")))
这里有几个例子说明正在使用的工作模式。 You can
看看它如何简单地说明什么是守则的障碍。
together.

==== 实例1:不动产

(( &quot; 工作模式单位 &quot; ,usingusing UoW to group multiple operations into Atomic unit", rereallocate function example")))
((分配服务职能)))
Suppose we want to be able to deallocate and then reallocate orders:

[[reallocate]]
.重新分配服务职能
====
[source,python]
[role="skip"]
----
def reallocate(
    line: OrderLine,
    uow: AbstractUnitOfWork,
) -> str:
    with uow:
        batch = uow.batches.get(sku=line.sku)
        if batch is None:
            raise InvalidSku(f'Invalid sku {line.sku}')
        batch.deallocate(line)  #<1>
        allocate(line)  #<2>
        uow.commit()
----
====

P1. 如果`deallocate()`失败,我们显然想叫`allocate()`。
P2,如果`allocate()`失败,我们可能不愿意实际承诺
    the `deallocate()` either.


==== Example 2: Change Batch Quantity

(( &quot; 工作模式单位 &quot; ,usingusing UoW to group multiple operations into Atomic unit", chanchanging batch quantity example")))
我们的航运公司向我们发出呼吁,说集装箱门之一
开放了,我们有一半的索法落入印度洋。 Oops!


[[change_batch_quantity]]
.变化数量
====
[source,python]
[role="skip"]
----
def change_batch_quantity(
    batchref: str, new_qty: int,
    uow: AbstractUnitOfWork,
):
    with uow:
        batch = uow.batches.get(reference=batchref)
        batch.change_purchased_quantity(new_qty)
        while batch.available_quantity < 0:
            line = batch.deallocate_one()  #<1>
        uow.commit()
----
====

.1. 在此,我们可能需要讨论任何种类。 If we get a failure
    在任何阶段,我们也许都希望作出任何改变。
    (( &quot; 工作模式单位 &quot; ,usingusing UoW to group multiple operations into Atomic unit", startref=Uix UoWatomtom)))
    ((atoatomic operations", usingusing 工作单元（Unit of Work） to group operations into Atomic unit", startref="ix atomops")))


=== 推进一体化测试

((testtestingtest, "工作单元（Unit of Work） with integration tests", titidying up tests"))
(( &quot; 工作模式单位 &quot; ,titidying up integration tests tests)))
我们现在有三套测试,基本上都放在数据库:
test orm.py ,  test repository.py , and  test uow.py . 如果我们投 throw任何东西
away?

====
[source,text]
[role="tree"]
----
└── tests
    ├── conftest.py
    ├── e2e
    │   └── test_api.py
    ├── integration
    │   ├── test_orm.py
    │   ├── test_repository.py
    │   └── test_uow.py
    ├── pytest.ini
    └── unit
        ├── test_allocate.py
        ├── test_batches.py
        └── test_services.py

----
====

如果你认为自己不想去做,你就应该永远感到可以放弃试验。
增加长期价值。 We'd say that  test orm.py  was primarily a tool to help
We learn SQLAlchemy, so we wonchet need that long term, especially if the main things
test test  doing  doing test . 上一次的考验,你可以保持在周围,
但是,我们当然可以看到一个论点,就是要把一切都保持在最崇高的位置。
可能的抽象程度(与我们进行单位测试时的合理性)。

[role="nobreakinside less_space"]
.退休人员的行使
******************************************************************************
就本章而言,最好的做法可能是执行一项建议。
UoW from scratch. The code, as always, is https://github.com/cosmicpython/code/tree/chapter_06_uow_exercise[on GitHub]. 你要么可以沿用我们非常密切的模式,
或可能试验分离遗体(其责任是谁)
`commit()`, `rollback()`, and providing the `.batches` Library) from the
环境管理人员,其工作是启动事情,然后做承诺
或退出。 If you feel like going all-functional rather than
你可以使用`@contextmanager`。
`contextlib`.

We Weve stripped out both the actual UoW and the fakes, as well as paring back
the abstract UoW. 如果你与你站在一起,为什么不把我们与你的回想联系起来。
你特别自豪?
******************************************************************************

TIP: This is another example of the lesson from <<chapter_05_high_gear_low_gear>>:
    在我们积累更好的抽象时,我们可以使我们的试验转向对抗它们,
    这使我们可以自由地改变基本细节。


=== Wrap-Up

(( &quot; 工作模式单位 &quot; , 使用的好处))
Hopefully we weve convinced you that the 工作单元（Unit of Work） pattern is useful, and
环境管理者是真正 n的病态
of visually grouping code into blocks that we want to happen Atomically.

((Session object"))
((QSQLAlchemy,,"Session object")))
事实上,这种模式非常有用,因为SQLAlchemy已经使用UoW
in the shape of the `Session` object. The `Session` object in SQLAlchemy is the way
That your application loads data from the database.

Every time you load a new entity from the database, the session begins to  track 
对实体的更改,在届会闭幕时,你们的所有变动都是
始终如一。 如果它已经实施我们想要的模式,我们为什么要努力抽象地取消质量问题会议?

(( &quot; 工作模式单位 &quot; 、propros and cons or trade-offs")))
<<chapter_06_uow_tradeoffs>>讨论一些权衡。

[[chapter_06_uow_tradeoffs]]
[options="header"]
.工作模式股:
|===
ProProsConCons
a a
* 我们对原子作业的概念和原子作业的概念有 abstract的抽象。
  视觉地看,环境管理人员很容易看到什么法律障碍。
  原统合。
  ((atoatomic operations", Uni工作单元（Unit of Work） as abstraction over over)))
  ((交易、工作单位和Uni))

* 我们明确控制了交易何时开始和完成,以及我们
  申请未能以违约方式安全。 我们从未感到担忧
  行动部分实施。

* 有一个 n子,可以把所有的宝库放置起来,以便客户代码。

* 正如你在以后各章中所看到的那样,原子关系只涉及交易;
  可以帮助我们处理各种事件和新闻媒介。

a a
* Your ORM probably already has some perfectly good abstractions around
  原子。 SQLAlchemy, even has context managers. You can go a long way
  just passing a session around.

* We Weve made it look easy, but you have to think quite carefully about
  roll动、多面包和nes息交易等。 或许正义
  sticking to what Django or Flask-SQLAlchemy gives you will keep your life
  较简单。
  (( &quot; 工作模式单位 &quot; ,开端=Uix UoW"))
|===

一方面, &quot; API &quot; 会议富有,支持我们所捐助的行动
我们希望或需要我们的领域。 我们的`UnitOfWork`将本届会议简化为会议
基本核心:可以启动、承诺或 throw弃。

另外,我们利用`UnitOfWork`进入我们的`仓储（Repository）`物体。
这是我们可以与一个平原做的开发者可用性的必要条件
SQLAlchemy `Session`.

[role="nobreakinside less_space"]
.工作单元（Unit of Work） Patcap
*****************************************************************
(( &quot; 工作模式单位 &quot; ,重要要点上限cap))

工作模式股围绕数据完整性进行抽象:
    It helps to enforce the consistency of our domain model, and improves
    业绩,让我们在行动中进行单一的氟化
    行动结束。

It works closely with the 仓储（Repository） and 服务层（Service Layer） patterns:
    工作模式股完成我们对数据获取的抽象
    代表原子更新。 每种服务使用个案都以一种方式运行
    能够成功或失败的单一工作单位。

This is a lovely case for a context manager:
    背景管理人员是平原界定范围的一个独特方式。 我们可以使用
    context manager to automatically roll our work at the end of a request,
    这意味着该系统通过违约而安全。

SQLAlchemy已经实施了这一模式:
    We introduce an even simpler abstraction over the SQLchemy `Session` object
    in order to "narrowrow the interface between the ORM and our code. This helps
    to keep us loosely coupled.

*****************************************************************

((独立转换原则))
最后,我们再次出于依赖性version依原则的动机:我们
服务层取决于抽象,我们附上具体内容
在系统外缘执行。 This lines up nicely with
SQLAlchemyches own
https://oreil.ly/tS0E0[recommendations]:

[quote, SQLALchemy "Session Basics" Documentation]
____
保持届会的周期(通常是交易)是分开的,
外部。 建议采取更实质性的最全面办法
申请将设法保存会议、交易和交易的细节
尽可能不考虑方案细节
其工作。
____


//IDEA:  not sure where, but we should maybe talk about the option of separating
// the uow into a uow plus a uowm.
